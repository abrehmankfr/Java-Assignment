var dice4Value = Math.floor(Math.random() * 4) + 1;

        // Simulating a 6-sided dice roll (generating a random number between 1 and 6)
        var dice6Value = Math.floor(Math.random() * 6) + 1;

        // Displaying the dice values in the browser
        document.write("<p>4-sided dice rolled: " + dice4Value + "</p>");
        document.write("<p>6-sided dice rolled: " + dice6Value + "</p>");